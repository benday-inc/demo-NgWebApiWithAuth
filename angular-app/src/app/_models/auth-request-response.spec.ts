import { AuthRequestResponse } from './auth-request-response';

describe('AuthRequestResponse', () => {
  it('should create an instance', () => {
    expect(new AuthRequestResponse()).toBeTruthy();
  });
});
