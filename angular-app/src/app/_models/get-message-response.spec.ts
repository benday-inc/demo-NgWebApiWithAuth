import { GetMessageResponse } from './get-message-response';

describe('GetMessageResponse', () => {
  it('should create an instance', () => {
    expect(new GetMessageResponse()).toBeTruthy();
  });
});
